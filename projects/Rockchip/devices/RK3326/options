################################################################################
# setup device defaults
#   https://www.rockchip.fr/RK3326%20datasheet%20V1.4.pdf
#     TARGET_CPU       "cortex-a35"
#     ATF_PLATFORM     "px30"
#     MALI_FAMILY      "bifrost-g31"
#   https://docs.mesa3d.org/drivers/panfrost.html
#     GRAPHIC_DRIVERS  "panfrost"
#     Remove EXTRA_CMDLINE
################################################################################

  # The TARGET_CPU variable controls which processor should be targeted for
  # generated code.
    case $TARGET_ARCH in
      aarch64)
        TARGET_CPU="cortex-a35"
        TARGET_CPU_FLAGS="+crc+crypto"
        ;;
      arm)
        TARGET_KERNEL_ARCH="arm64"
        TARGET_FLOAT="hard"
        TARGET_CPU="cortex-a35"
        TARGET_CPU_FLAGS="+crc"
        TARGET_FPU="crypto-neon-fp-armv8"
        ;;
    esac

  # Firmware
    UBOOT_FIRMWARE+=" atf"
    ATF_PLATFORM="px30"
    ATF_BL31_BINARY="bl31/bl31.elf"

  # Kernel target
    KERNEL_TARGET="Image"

  # Mali GPU family
    MALI_FAMILY="g31"
    GRAPHIC_DRIVERS="mali panfrost"

  # OpenGL-ES implementation to use
    OPENGLES="mesa"

  # Specific to ODROID-GO Advance
    CEC_FRAMEWORK_SUPPORT="no"

  # Displayserver to use (wl / x11 / no)
    DISPLAYSERVER="wl"

  # Windowmanager to use (fluxbox / sway / weston / no)
    WINDOWMANAGER="sway"

  # additional packages to install
    ADDITIONAL_PACKAGES+=" odroidgo2-utils odroidgoa-volkeys usb-modeswitch-RK3326"

  # additional Firmware to use (dvb-firmware, misc-firmware, wlan-firmware)
  # Space separated list is supported,
  # e.g. FIRMWARE="dvb-firmware misc-firmware wlan-firmware"
    FIRMWARE="rk3326-firmware realtek-wifi-firmware"

  # bootargs for release
    EXTRA_CMDLINE="console=tty0 net.iframes=0 fbcon=rotate:3"
  # bootargs with serial console enable for debug
  # EXTRA_CMDLINE="console=ttyS2,1500000n8 console=tty0 net.iframes=0 fbcon=rotate:3 systemd.debug_shell=ttyS2"

  # set the addon project
    ADDON_PROJECT="ARMv8"
