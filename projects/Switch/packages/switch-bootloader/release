#!/bin/sh

. config/options $1

mkdir -p $RELEASE_DIR/3rdparty/bootloader

cp -PR $INSTALL/usr/share/bootloader/tegra210-nintendo-switch.dtb $RELEASE_DIR/3rdparty/bootloader
cp -PR $INSTALL/usr/share/bootloader/boot.scr $RELEASE_DIR/3rdparty/bootloader
