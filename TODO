ncurses-5.8/5.9:
  - segfaults in nano and dialog -> using ncurses-5.7

irserver:
  - rework irserver/init.d for systemd

buildsystem:
  - always use fakeroot for converted packages
  - built-in $PKG_DIR/unpack replacement
  - rm -rf .$TARGET_NAME/ by default for converted packages
  - rm -rf $INSTALL/ by edfault for converted packages
  - always trigger full pkg rebuild (unpack+build) on meta/package.mk change
    -- we can use this even when we drop a new patch in patches/. or when we
       require a rebuild for some other reason. with a simple PKG_REV bump
