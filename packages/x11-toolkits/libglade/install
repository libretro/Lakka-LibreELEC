#!/bin/sh

. config/options

$SCRIPTS/install Python
$SCRIPTS/install gtk+

mkdir -p $INSTALL/usr/lib
cp -PR $PKG_BUILD/glade/.libs/*.so* $INSTALL/usr/lib

mkdir -p $INSTALL/usr/share/xml/$1
cp -PR $PKG_BUILD/*.dtd $INSTALL/usr/share/xml/$1
