#!/bin/sh

. config/options

$SCRIPTS/install Python
$SCRIPTS/install gtk+

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/glade/.libs/*.so* $INSTALL/usr/lib

mkdir -p $INSTALL/usr/share/xml/$1
cp -PR $BUILD/$1*/*.dtd $INSTALL/usr/share/xml/$1
