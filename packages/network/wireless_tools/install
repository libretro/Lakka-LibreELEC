#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin
cp $PKG_BUILD/iwconfig $PKG_BUILD/iwpriv $INSTALL/usr/bin
[ "$DEVTOOLS" = yes ] && cp $PKG_BUILD/iwlist $INSTALL/usr/bin

mkdir -p $INSTALL/usr/lib
cp -PR $PKG_BUILD/libiw.so* $INSTALL/usr/lib

exit 0
