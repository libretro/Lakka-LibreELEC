#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin
  cp $BUILD/$1*/iwconfig $BUILD/$1*/iwpriv $INSTALL/usr/bin
  [ "$DEVTOOLS" = yes ] && cp $BUILD/$1*/iwlist $INSTALL/usr/bin

mkdir -p $INSTALL/usr/lib
  cp -PR $BUILD/$1*/libiw.so* $INSTALL/usr/lib

exit 0
