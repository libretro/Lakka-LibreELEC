#!/bin/sh

. config/options

mkdir -p $INSTALL/firmware
cp -PR $BUILD/$1*/* $INSTALL/firmware

mkdir -p $INSTALL/firmware/zd1211
mv $INSTALL/firmware/zd1211_* $INSTALL/firmware/zd1211
mv $INSTALL/firmware/zd1211b_* $INSTALL/firmware/zd1211
