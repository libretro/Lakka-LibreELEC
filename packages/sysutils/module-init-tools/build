#!/bin/sh

. config/options

$SCRIPTS/build toolchain

export CFLAGS="-Os"

cd $PKG_BUILD

mkdir -p .build-target
cd .build-target

../configure --host=$TARGET_NAME \
             --build=$HOST_NAME \

make modprobe
make depmod
