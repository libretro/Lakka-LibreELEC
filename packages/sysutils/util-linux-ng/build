#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --with-fsprobe=builtin

make -C libs/blkid

$MAKEINSTALL -C libs/blkid
