# start udev daemon
#
# runlevels: openelec, text, debug

(
  progress "starting Udev"

  echo > /proc/sys/kernel/hotplug

  udevd --daemon
  udevadm trigger
  udevadm settle --timeout=60
  udevadm monitor 2>&1 >/var/log/udev.log
)&
