#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build zlib

cd $PKG_BUILD
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \

make

#cp -f usb.ids $SYSROOT_PREFIX/usr/share/
