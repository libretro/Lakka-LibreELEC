#!/bin/sh

. config/options $1

$SCRIPTS/install make

setup_toolchain host
CC=$LOCAL_CC

cd $PKG_BUILD
./configure --host=$HOST_NAME \
            --build=$HOST_NAME \
            --prefix=$ROOT/$TOOLCHAIN
make
