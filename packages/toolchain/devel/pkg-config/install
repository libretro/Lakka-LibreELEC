#!/bin/sh

. config/options $1

mkdir -p $ROOT/$TOOLCHAIN/bin
  cp $PKG_BUILD/$1 $ROOT/$TOOLCHAIN/bin

mkdir -p $SYSROOT_PREFIX/usr/share/aclocal
  cp $PKG_BUILD/pkg.m4 $SYSROOT_PREFIX/usr/share/aclocal