#!/bin/sh

. config/options

setup_toolchain host

cd $PKG_BUILD
./configure --prefix=/usr \
            --sysconfdir=/etc \
            --with-pc-path="$SYSROOT_PREFIX/usr/lib/pkgconfig" \
            --enable-static
make
