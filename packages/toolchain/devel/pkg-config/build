#!/bin/sh

. config/options

setup_toolchain host

cd $BUILD/$1*
./configure --prefix=/usr \
            --sysconfdir=/etc \
            --with-pc-path="$SYSROOT_PREFIX/usr/lib/pkgconfig" \
            --enable-static
make
