#!/bin/sh

. config/options $1

$SCRIPTS/install font-cursor-misc
$SCRIPTS/install font-util

mkdir -p $INSTALL/$XORG_PATH_FONTS/misc
cp $PKG_BUILD/fonts/* $INSTALL/$XORG_PATH_FONTS/misc
cp $PKG_DIR/config/fonts.alias $INSTALL/$XORG_PATH_FONTS/misc
