#!/bin/sh

. config/options

mkdir -p $INSTALL/$XORG_PATH_ENCODINGS
cp $BUILD/$1*/*.enc $INSTALL/$XORG_PATH_ENCODINGS

mkdir -p $INSTALL/$XORG_PATH_ENCODINGS/large
cp $BUILD/$1*/large/*.enc $INSTALL/$XORG_PATH_ENCODINGS/large
