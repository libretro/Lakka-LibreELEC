#!/bin/sh

. config/options
$SCRIPTS/install zlib

mkdir -p $INSTALL/usr/lib
cp -PR $PKG_BUILD/src/.libs/libpciaccess.so* $INSTALL/usr/lib

mkdir -p $INSTALL/usr/bin
cp -PR $PKG_BUILD/src/.libs/scanpci $INSTALL/usr/bin

