#!/bin/sh

. config/options

$SCRIPTS/install libX11
$SCRIPTS/install libXv

mkdir -p $INSTALL/usr/lib
cp -P $PKG_BUILD/src/.libs/libXvMC.so* $INSTALL/usr/lib
cp -P $PKG_BUILD/src/.libs/libXvMCW.so* $INSTALL/usr/lib
