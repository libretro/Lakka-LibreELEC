#!/bin/sh

. config/options

$SCRIPTS/install libXau

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/src/.libs/libX11*.so* $INSTALL/usr/lib
rm -rf $INSTALL/usr/lib/libX11*.so*T

mkdir -p $INSTALL/usr/share/X11
cp -PR $BUILD/$1*/src/XKeysymDB $INSTALL/usr/share/X11
