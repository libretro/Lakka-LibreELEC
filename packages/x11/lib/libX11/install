#!/bin/sh

. config/options $1

$SCRIPTS/install libXau
$SCRIPTS/install libxcb

mkdir -p $INSTALL/usr/lib
  cp -PR $PKG_BUILD/src/.libs/libX11*.so* $INSTALL/usr/lib
  rm -rf $INSTALL/usr/lib/libX11*.so*T

mkdir -p $INSTALL/usr/share/X11
  cp -PR $PKG_BUILD/src/XKeysymDB $INSTALL/usr/share/X11
