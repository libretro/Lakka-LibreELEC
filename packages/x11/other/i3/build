#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libX11
#$SCRIPTS/build imlib2

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --sysconfdir=/etc \
            --localstatedir=/var \

make

$STRIP src/$1
