#!/bin/sh

. config/options $1

$SCRIPTS/build toolchain
$SCRIPTS/build libxml2

cd $PKG_BUILD

$AUTORECONF

./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \

make
