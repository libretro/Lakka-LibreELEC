#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build ncurses

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --sysconfdir=/etc \
            --enable-static \
            --disable-shared \
            --with-curses

make

$MAKEINSTALL