#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build ncurses

cd $PKG_BUILD
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --sysconfdir=/etc \
            --enable-static \
            --disable-shared \
            --with-curses

make

$MAKEINSTALL