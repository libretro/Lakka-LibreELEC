#!/bin/sh

. config/options

$SCRIPTS/install zlib
$SCRIPTS/install freetype

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/src/.libs/libfontconfig.so* $INSTALL/usr/lib
mkdir -p $INSTALL/usr/bin
cp -PR $BUILD/$1*/fc-cache/.libs/fc-cache $INSTALL/usr/bin
mkdir -p $INSTALL/etc/fonts
cp -R $SYSROOT_PREFIX/etc/fonts/* $INSTALL/etc/fonts
