#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $PKG_BUILD
./configure --prefix=/usr \
            --disable-static \
            --enable-shared \
            --disable-debug \
            --cross-compile \
            --disable-doc \
            --enable-logcolor \

make

$MAKEINSTALL