#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libraw1394

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --sysconfdir=/etc \
            --enable-static \
            --disable-shared \

make

#$STRIP src/.libs/*.so*

$MAKEINSTALL
