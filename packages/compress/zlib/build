#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $PKG_BUILD
./configure --prefix=/usr \
            --libdir=/usr/lib \
            --includedir=/usr/include \
            --shared

make

$MAKEINSTALL
