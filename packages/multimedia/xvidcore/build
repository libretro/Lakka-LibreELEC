#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/$1*/build/generic

./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-static \
            --disable-shared

make

#$STRIP ./\=build/*.so*

$MAKEINSTALL
