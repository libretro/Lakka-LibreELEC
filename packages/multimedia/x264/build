#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $PKG_BUILD
./configure --host=$TARGET_NAME \
            --prefix=/usr \
            --enable-mp4-output \
            --enable-pthread \
            --enable-shared \
            --disable-asm
make

$STRIP

$MAKEINSTALL
