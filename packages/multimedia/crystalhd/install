#!/bin/sh

. config/options

mkdir -p $INSTALL/lib/firmware
  cp -P $PKG_BUILD/firmware/fwbin/70012/bcm70012fw.bin $INSTALL/lib/firmware

mkdir -p $INSTALL/usr/lib
  cp -P $PKG_BUILD/linux_lib/libcrystalhd/*.so* $INSTALL/usr/lib

mkdir -p $INSTALL/lib/udev/rules.d
  cp -P $PKG_BUILD/driver/linux/*.rules $INSTALL/lib/udev/rules.d
