#!/bin/sh

. config/options

$SCRIPTS/install gstreamer
#$SCRIPTS/install libsidplay

mkdir -p $INSTALL/usr/lib/gstreamer-0.10
cp -PR `find $BUILD/$1*/{ext,gst} -name *.so` $INSTALL/usr/lib/gstreamer-0.10
