#!/bin/sh

. config/options $1

$SCRIPTS/build toolchain
$SCRIPTS/build libX11

cd $PKG_BUILD
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-shared \
            --disable-static \
            --disable-documentation \

make

$MAKEINSTALL
