function custom_post_commit() {
  echo "Starting update of rust sub-packages to $pkgver."
  export run_sub_package=1
  ${progname} cargo-snapshot ${pkgver}
  ${progname} rustc-snapshot ${pkgver}
  ${progname} rust-std-snapshot ${pkgver}
}
